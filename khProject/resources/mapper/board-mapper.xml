<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="boardMapper">
 
 	<select id="selectListCount" resultType="_int">
 		SELECT
 			   COUNT(*)
 		  FROM
 		       KH_BOARD
 		 WHERE
 		       STATUS = 'Y'
 		   AND
 		       BOARD_TYPE = 1
 	</select>
  
  	<select id="selectBoardList" parameterType="com.kh.java.common.vo.PageInfo"
  			resultType="com.kh.java.board.model.vo.Board">
  		SELECT
  			   BOARD_NO boardNo
  			 , CATEGORY_NAME category
  			 , USER_NAME boardWriter
  			 , BOARD_TITLE boardTitle
  			 , CREATE_DATE createDate
  			 , COUNT
  	     FROM
  	           KH_BOARD
  	     JOIN
  	           KH_CATEGORY USING (CATEGORY_NO)
  	     JOIN
  	           KH_MEMBER ON (BOARD_WRITER = USER_NO)
  	    WHERE
  	           KH_BOARD.STATUS = 'Y'
  	      AND
  	           BOARD_TYPE = 1
  	    ORDER
  	       BY
  	           BOARD_NO DESC
  	   OFFSET  #{offset} ROWS FETCH NEXT #{boardLimit} ROWS ONLY
  	</select>
  	
  	<select id="selectCategory" resultType="com.kh.java.board.model.vo.Category">
  		SELECT
  		       CATEGORY_NO categoryNo
  		     , CATEGORY_NAME categoryName
  		  FROM
  		       KH_CATEGORY
  		 ORDER
  		    BY
  		       CATEGORY_NO ASC
  	</select>
  </mapper>